line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "I", "B", "SIM", "UP"]
ignore = [
    # `Depends(...)` in a parameter default is the documented FastAPI idiom.
    "B008",
    "E501",
]

[lint.per-file-ignores]
"tests/*" = ["B904", "SIM117"]
