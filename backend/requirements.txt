# Core application
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
sqlalchemy>=2.0
python-dotenv>=1.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 cannot read the version of bcrypt 4.1+, which produces a noisy
# (but harmless) warning on every hash. Pinning below it keeps startup clean.
bcrypt<4.1
pyjwt>=2.8
httpx>=0.27
networkx>=3.2

# Source parsing
tree-sitter>=0.21
tree-sitter-python>=0.21
tree-sitter-javascript>=0.21
tree-sitter-java>=0.21
# Optional but recommended: without it .ts/.tsx parse with the JavaScript
# grammar and their type annotations are invisible.
tree-sitter-typescript>=0.21

# Model providers (optional -- the app runs deterministically without either)
openai>=1.30
tiktoken>=0.6
# anthropic>=0.40

# Tests
pytest>=8.0
